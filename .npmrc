# Use Liara npm mirror — required because registry.npmjs.org is unreachable
# from Liara's build servers. This ensures any npm install command
# (including Liara's internal scripts like configure_prisma.sh)
# uses the Liara mirror.
registry=https://npm.liara.ir
